      1  var gridRow = 
      2  var gridCol = 

      3  var showDiv = 
      4  var showGIF = 
      5  var cardBody = ;
      6  var p = 

      
      

        showDiv.append(cardBody);
        cardBody.append(p);
        showDiv.prepend(showGIF);
        $('.gifs-load-here').append(showDiv);

        gridRow.prepend(gridCol)
        gridCol.append(showDiv)
        showDiv.append(showGIF)
        showGIF.append(cardBody)
        cardBody.append(p)
        
        

        $('.gifs-load-here').append(gridRow);

